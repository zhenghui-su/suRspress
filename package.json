{
  "name": "rspress-doc-template",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "rspress dev",
    "build": "rspress build",
    "preview": "rspress preview --port 8080"
  },
  "dependencies": {
    "rspress": "^0.1.1"
  },
  "devDependencies": {
    "@types/node": "^14"
  }
}
